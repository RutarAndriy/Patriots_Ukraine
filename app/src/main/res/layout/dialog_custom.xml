<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:text="Custom Message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/dialog_message"
        android:textSize="@dimen/dialog_message_size"
        android:textColor="?attr/text_color_2"
        android:paddingTop="@dimen/dialog_m_padding_top"
        android:paddingBottom="@dimen/dialog_m_padding_bottom"
        android:paddingLeft="@dimen/dialog_h_padding"
        android:paddingRight="@dimen/dialog_h_padding" />
</FrameLayout>